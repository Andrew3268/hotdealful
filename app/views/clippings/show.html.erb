<% if current_user && current_user.admin %>
  <p>
    <strong>Cli title:</strong>
    <%= @clipping.cli_title %>
  </p>

  <%= link_to 'Edit', edit_clipping_path(@clipping) %> |
  <%= link_to 'Back', clippings_path %>
<% end %>