<% if current_user && current_user.admin %>
  <div class="wrap">
    <h1>Editing Post</h1>
    <%= render 'form', post: @post %>
    <%= link_to 'Show', @post %> |
    <%= link_to 'Back', posts_path %>
  </div>
<% end %>