<% if current_user && current_user.admin %>
  <div class="wrap">
    <h1>Editing Deal</h1>
    <%= render 'form', deal: @deal %>
    <%= link_to 'Show', @deal %> |
    <%= link_to 'Back', deals_path %>
  </div>
<% end %>